# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12

# --- ML ---
# Pinned to versions known to work together on CPU.
# When you flip inference_device to "cuda", swap torch for a CUDA build.
numpy==1.26.4
pillow==11.0.0
opencv-python-headless==4.10.0.84
torch==2.5.0
onnxruntime==1.19.2
insightface==0.7.3
# Age estimation: we use the genderage head bundled with InsightFace's
# `buffalo_l` pack. No separate model dependency. Earlier plans considered
# MiVOLO (research-only weight license) and DeepFace (TensorFlow bloat
# + Windows weight-download issues). See WEEK1-04 Completion Log for
# the rationale. To swap in a heavier dedicated age model later, see
# `app/ml/pipeline.py::AgePipeline.estimate_age`.

# --- Database / Auth ---
supabase==2.9.0
python-jose[cryptography]==3.3.0

# --- Observability ---
python-json-logger==2.0.7

# --- Dev / Test ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.7.0
